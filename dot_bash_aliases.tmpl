# =========================
# Bash 专属附加配置
# Debian/Ubuntu 的默认 ~/.bashrc 会自动加载本文件；
# Fedora/RHEL 系的默认 bashrc 不读取本文件，改由 ~/.bashrc.d/10-dotfiles.sh 加载。
# 两份内容一致，均来自 .chezmoitemplates/bash-config。
# =========================
{{ template "bash-config" . }}
