# =========================
# Bash 配置（Fedora/RHEL 系加载路径）
# Fedora/RHEL 系默认 ~/.bashrc 只加载 ~/.bashrc.d/*，不读取 ~/.bash_aliases。
# 内容与 ~/.bash_aliases 一致，均来自 .chezmoitemplates/bash-config。
# =========================
{{ template "bash-config" . }}
